import AdminLayout from "../components/AdminLayout";

export function SettingsPlaceholder() {
  return (
    <AdminLayout>
      <h1 className="admin-page-title">Settings</h1>
      <p className="admin-page-subtitle">Studio-wide settings — coming in a later phase.</p>
      <div className="empty-state">Not built yet.</div>
    </AdminLayout>
  );
}
